import { SyncProviderStatusPipe } from './sync-provider-status.pipe';

describe('SyncProviderStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new SyncProviderStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
